# 変更履歴

## 1.3.0

### 追加
- `.bat` の実行後に、元ファイルを削除するか確認するようにした（既定は削除、`n` で残す）
- `.bat` 自身を削除するかも確認するようにした（既定は削除、`n` で残す）
  - 削除は出力ファイルができたことを確認してからのみ行う
  - 自身の削除は最終行で行い、ラベルなしの `(goto)` でバッチを打ち切ってから消す。
    そのまま `del` すると「The batch file cannot be found.」が表示されるため
- 保存後に実際の保存先フォルダーをポップアップに表示するようにした
- 設定画面に、保存先が「Chrome のダウンロード フォルダー + サブフォルダ」になる旨の説明を追加

### 補足
- 拡張機能から保存先に絶対パスは指定できない（Chrome が `Invalid filename` で拒否する）。
  `N:\Videos\MediaGrabber` に保存したい場合は、Chrome 側のダウンロード先を
  `N:\Videos` にしたうえで、サブフォルダを `MediaGrabber` にする

## 1.2.1

### 追加
- 結合用 `.bat` に音ズレ補正を追加。実行時に秒数を入力すると `-itsoffset` で補正する
  - 正の値: 音声を遅らせる（音声が先に聞こえるとき）
  - 負の値: 音声を早める。音声側を負にずらすとタイムスタンプが 0 に丸められるため、
    実際には映像側を遅らせて相対差を作る
- 保存名と補正値をコマンドライン引数でも渡せるようにした（`bat 名前 補正値`）

### 修正
- `echo` 行に含めていた `->` の `>` がリダイレクトとして解釈され、行が壊れていた

## 1.2.0

### 追加
- 保存と同時に、MP4 にするための Windows バッチファイルを一緒に保存するようにした
  - 映像と音声が別ファイルになった場合: `〜.結合.bat`（ダブルクリックで結合）
  - `.ts` 1 本で保存された場合: `〜.変換.bat`（ダブルクリックで MP4 化）
  - どちらも実行時に保存名を入力できる（Enter でそのまま、拡張子は自動で補う）
  - 既存ファイルがある場合は上書き確認する
  - いずれも `-c copy` のため再エンコードされず、画質・音質は劣化しない
- 設定に「MP4 にする .bat を一緒に保存する」を追加（既定で有効）

### 補足
- バッチファイルでは `goto` を使っていない。cmd.exe は `goto` の飛び先をバイト位置で
  探すため、日本語を含むファイルでは文字の途中に着地して行が壊れることがある

## 1.1.0

### 追加
- 一覧に**サムネイル**と**再生時間バッジ**を表示。再生中の映像からコマを取り出し、
  取得できない場合は `poster` 属性 → OGP 画像の順に代替する
- ポップアップを開いた時点で各プレイリストを**自動で確認**し、
  「マスター（N 画質を選択可能）」か「単一画質の再生リスト」かを表示する
- マスターに「推奨」マークを付け、マスターに含まれる再生リストは下にまとめて控えめに表示する
- 画質ごとの**推定サイズ**（ビットレート×再生時間）、長さ、セグメント数を表示
- `popup.html?tabId=<id>` でタブとして開けるようにした（動作確認用）

### 修正
- HLS / DASH のサイズ表示が動画のサイズだと誤解される問題。
  再生リスト自体のサイズであることを `再生リスト 597 B` のように明示するようにした
- マニフェストの取得がタイトル通知より先に届いた場合に、一覧の見出しがファイル名になっていた
- 画質確認時にも Referer を付与するようにした（Referer を要求する CDN で確認が失敗していた）
- 暗号化された配信と判明している場合はダウンロードボタンを無効化し、
  押すたびにエラーが積み上がらないようにした

## 1.0.0

- 初版。直リンク（MP4 / WebM など）、HLS、DASH の検出と保存に対応
- 画質選択、同時ダウンロード数・保存先の設定、進捗表示と中断
- 映像と音声が分離している配信では 2 ファイルで保存し、結合用の ffmpeg コマンドを案内
- DRM 保護コンテンツおよび暗号化された HLS は非対応（意図的に実装していない）
